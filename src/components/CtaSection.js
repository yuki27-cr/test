export function CtaSection() {
  return `
    <section id="pricing" class="cta-section">
      <div>
        <div class="section-kicker">Start today</div>
        <h2>One workspace for the product work behind every release.</h2>
      </div>
      <a class="primary-button large" href="mailto:hello@example.com">Request access</a>
    </section>
  `;
}
